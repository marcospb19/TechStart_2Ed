"""
This is the controllers module, in each submodule there is a implementation for
the 4 main CRUD operations.

Submodules:
    Category
    Marketplace
    Product
    Seller

Operations available (in each submodule):
    @router.post   def create()
    @router.get    def show()
    @router.put    def update()
    @router.delete def delete()

The methods implemented are supposed to be called only by ../routers/*.py
correspondent files.
"""
