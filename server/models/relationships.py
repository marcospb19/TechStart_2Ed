"""
This is the relationships model submodule.

It was supposed to make the ManyToMany relation between products and categories.
"""

# from db import Base
# from sqlalchemy import Table, Column, Integer, ForeignKey

# association_table = Table(
#     'association',
#     Base.metadata,
#     Column('products_id', Integer, ForeignKey('products.id')),
#     Column('categories_id', Integer, ForeignKey('categories.id')),
# )
